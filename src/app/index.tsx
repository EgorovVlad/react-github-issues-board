import { ToastContainer } from "react-toastify";

export type AppProps = {};

export const App: React.FC<AppProps> = () => {
  return (
    <>
      <ToastContainer />
    </>
  );
};

export default App;
